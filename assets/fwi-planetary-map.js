/* FutureWorld Intelligence Planetary Computer map integration v1.2 */
(function(){
  'use strict';

  const STAC_API='https://planetarycomputer.microsoft.com/api/stac/v1';
  const DATA_API='https://planetarycomputer.microsoft.com/api/data/v1';
  const THEME_BASE='/pages/climate/evidence-explorer/themes/';
  const MAX_LONGITUDE_SPAN=12;
  const MAX_LATITUDE_SPAN=9;
  const REQUEST_TIMEOUT=45000;

  const PRESETS={
    global:{label:'Global view',center:[20,0],zoom:2,bbox:[-179,-75,179,75]},
    pakistan:{label:'Pakistan',center:[30.4,69.3],zoom:5,bbox:[60.8,23.6,77.9,37.1]},
    kp:{label:'Khyber Pakhtunkhwa',center:[34.2,71.7],zoom:7,bbox:[69.15,31.0,74.15,36.95]},
    merged:{label:'Merged Districts',center:[33.5,70.6],zoom:8,bbox:[69.15,31.0,72.65,35.35]},
    bajaur:{label:'Bajaur pilot extent',center:[34.72,71.54],zoom:10,bbox:[71.10,34.45,71.95,35.05]}
  };

  const COLLECTIONS={
    'sentinel-2-l2a':{label:'Sentinel-2 L2A',cloud:true},
    'landsat-c2-l2':{label:'Landsat Collection 2 Level-2',cloud:true}
  };

  const LEGENDS={
    ndmi:{title:'Moisture condition (NDMI)',unit:'Index from -1 to +1',rows:[
      {color:'#8c510a',label:'Very low moisture signal',range:'below -0.20'},
      {color:'#d8b365',label:'Low moisture signal',range:'-0.20 to 0.10'},
      {color:'#f6e8c3',label:'Transitional / mixed',range:'0.10 to 0.25'},
      {color:'#5ab4ac',label:'Moderate moisture signal',range:'0.25 to 0.40'},
      {color:'#01665e',label:'High moisture signal',range:'above 0.40'}
    ],howTo:'Brown tones indicate a weaker moisture signal in vegetation or exposed surfaces. Blue-green tones indicate a stronger moisture signal.',warning:'NDMI is a surface and vegetation moisture indicator. It is not, by itself, a declaration of meteorological, agricultural or household water drought.'},
    ndvi:{title:'Vegetation condition (NDVI)',unit:'Index from -1 to +1',rows:[
      {color:'#a50026',label:'Very sparse / non-vegetated',range:'below 0.10'},
      {color:'#f46d43',label:'Sparse vegetation signal',range:'0.10 to 0.25'},
      {color:'#fee08b',label:'Moderate vegetation signal',range:'0.25 to 0.45'},
      {color:'#66bd63',label:'Healthy vegetation signal',range:'0.45 to 0.65'},
      {color:'#006837',label:'Very strong vegetation signal',range:'above 0.65'}
    ],howTo:'Red and orange tones indicate little vegetation response. Green tones indicate a stronger vegetation response for the selected period.',warning:'Low NDVI may reflect season, bare land, settlements, harvested crops, fire, cloud contamination or drought. Field and time-series checks are required.'},
    context:{title:'Satellite context',unit:'Natural-colour imagery',rows:[],howTo:'The map shows the landscape as seen by the selected satellite rendering. Use roads, settlements, vegetation and surface-water features for orientation.',warning:'Natural-colour imagery does not directly measure drought, emissions, vulnerability or policy implementation.'}
  };

  const INDICATORS={
    'water-drought':[
      {key:'ndmi',label:'Moisture condition — NDMI',collection:'sentinel-2-l2a',legend:'ndmi',lockCollection:true,evidenceClass:'Operational satellite indicator',explanation:'NDMI compares near-infrared and short-wave infrared reflectance to show relative moisture conditions in vegetation and exposed surfaces.',render:{name:'Normalized Difference Moisture Index (NDMI)',description:'Sentinel-2 B08 and B11 moisture index',options:'assets=B08&assets=B11&expression=(B08-B11)/(B08+B11)&asset_as_band=true&rescale=-0.8,0.8&colormap_name=BrBG'},requiredEvidence:'Confirm drought with rainfall, climatic water balance, soil-moisture, streamflow, groundwater and local water-access evidence.'},
      {key:'ndvi',label:'Vegetation condition — NDVI',collection:'sentinel-2-l2a',legend:'ndvi',lockCollection:true,evidenceClass:'Supporting satellite indicator',explanation:'NDVI compares near-infrared and red reflectance to show vegetation response and possible stress patterns.',render:{name:'Normalized Difference Vegetation Index (NDVI)',description:'Sentinel-2 B08 and B04 vegetation index',options:'assets=B08&assets=B04&expression=(B08-B04)/(B08+B04)&asset_as_band=true&rescale=-0.2,0.9&colormap_name=RdYlGn'},requiredEvidence:'Compare the same season across years and combine with rainfall, land use, fire and field observations.'},
      {key:'context',label:'Natural-colour satellite context',collection:'sentinel-2-l2a',legend:'context',lockCollection:false,evidenceClass:'Visual context',explanation:'A recognisable satellite view for orientation and checking visible land, vegetation and water features.',renderMatcher:/natural color|true color/i,requiredEvidence:'Use an indicator layer for interpretation; natural colour alone does not quantify drought.'}
    ],
    'forests-land-cover':[
      {key:'ndvi',label:'Vegetation condition — NDVI',collection:'sentinel-2-l2a',legend:'ndvi',lockCollection:true,evidenceClass:'Supporting satellite indicator',explanation:'NDVI highlights vegetation response for forest and land-condition inspection.',render:{name:'Normalized Difference Vegetation Index (NDVI)',description:'Sentinel-2 B08 and B04 vegetation index',options:'assets=B08&assets=B04&expression=(B08-B04)/(B08+B04)&asset_as_band=true&rescale=-0.2,0.9&colormap_name=RdYlGn'},requiredEvidence:'Confirm forest change with comparable dates, classification, official boundaries and field evidence.'},
      {key:'context',label:'Natural-colour satellite context',collection:'sentinel-2-l2a',legend:'context',lockCollection:false,evidenceClass:'Visual context',explanation:'A recognisable satellite view for forest and land-cover orientation.',renderMatcher:/natural color|true color/i,requiredEvidence:'Visible differences require seasonal and analytical validation.'}
    ],
    default:[{key:'context',label:'Satellite context',collection:'sentinel-2-l2a',legend:'context',lockCollection:false,evidenceClass:'Visual context only',explanation:'The current public map supplies satellite context while a validated thematic layer is prepared.',renderMatcher:/natural color|true color/i,requiredEvidence:'This theme requires additional authoritative indicator, risk or policy datasets.'}]
  };

  const THEMES={
    'temperature-emissions':{label:'Global temperature and emissions trends',type:'Indicator evidence',note:'Temperature and emissions require authoritative time-series and inventory datasets. The current map supplies geographic context only.'},
    'forests-land-cover':{label:'Forests and land-cover change',type:'Live map indicator',note:'Use vegetation condition and natural-colour context to inspect landscapes; confirm change through validated classification and field evidence.'},
    'water-drought':{label:'Water stress and drought',type:'Live pilot indicator',note:'Use NDMI and NDVI to inspect moisture and vegetation response. A complete drought assessment still requires rainfall, water-balance, hydrology and local evidence.'},
    'wildfires-burned-areas':{label:'Wildfires and burned areas',type:'Thematic layer pathway',note:'Current imagery can support before-and-after inspection. Validated active-fire and burned-area products remain required.'},
    'biodiversity-ecosystems':{label:'Biodiversity and ecosystem condition',type:'Evidence pathway',note:'Imagery supports habitat and vegetation context. Biodiversity conclusions require ecological indicators, species evidence and field verification.'},
    'glaciers-snow-mountains':{label:'Glaciers, snow and mountain systems',type:'Thematic layer pathway',note:'Seasonal imagery supplies context. Glacier and snow trends require dedicated cryosphere time series and elevation data.'},
    'hazards-vulnerability':{label:'Climate hazards and vulnerability',type:'Risk evidence',note:'The map is not an early-warning system. Risk analysis requires hazard, exposure, population, infrastructure and vulnerability datasets.'},
    'commitments-implementation':{label:'International climate commitments and implementation',type:'Policy evidence',note:'This theme is primarily documentary and institutional; use the thematic guide for NDCs, adaptation, finance and implementation evidence.'}
  };

  let instanceCounter=0;
  function pad(value){return String(value).padStart(2,'0')}
  function isoDate(date){return `${date.getFullYear()}-${pad(date.getMonth()+1)}-${pad(date.getDate())}`}
  function defaultDates(){const end=new Date();end.setDate(end.getDate()-5);const start=new Date(end);start.setDate(start.getDate()-120);return {start:isoDate(start),end:isoDate(end)}}
  function escapeHtml(value){return String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]))}
  async function requestJson(url,options={}){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),REQUEST_TIMEOUT);try{const response=await fetch(url,{...options,signal:controller.signal});if(!response.ok){let detail='';try{detail=(await response.text()).slice(0,240)}catch(error){}throw new Error(`Request failed (${response.status})${detail?`: ${detail}`:''}`)}return await response.json()}finally{clearTimeout(timer)}}
  function indicatorList(themeKey){return INDICATORS[themeKey]||INDICATORS.default}
  function indicatorByKey(themeKey,key){return indicatorList(themeKey).find(item=>item.key===key)||indicatorList(themeKey)[0]}

  function makeShell(root,id,dates,themeKey){
    const firstIndicator=indicatorList(themeKey)[0];
    root.innerHTML=`<div class="fwi-pc-map-shell">
      <div class="fwi-pc-map-toolbar" aria-label="Planetary Computer map controls">
        <label class="fwi-pc-theme-control"><span>Evidence theme</span><select data-pc-control="theme">${Object.entries(THEMES).map(([key,theme])=>`<option value="${key}"${key===themeKey?' selected':''}>${escapeHtml(theme.label)}</option>`).join('')}</select></label>
        <label class="fwi-pc-indicator-control"><span>Indicator</span><select data-pc-control="indicator">${indicatorList(themeKey).map(item=>`<option value="${item.key}">${escapeHtml(item.label)}</option>`).join('')}</select></label>
        <label><span>Geography</span><select data-pc-control="geography">${Object.entries(PRESETS).map(([key,preset])=>`<option value="${key}">${preset.label}</option>`).join('')}</select></label>
        <label><span>Dataset</span><select data-pc-control="collection">${Object.entries(COLLECTIONS).map(([key,item])=>`<option value="${key}">${item.label}</option>`).join('')}</select></label>
        <label><span>From</span><input type="date" data-pc-control="start" value="${dates.start}"></label>
        <label><span>To</span><input type="date" data-pc-control="end" value="${dates.end}"></label>
        <label><span>Maximum cloud <output data-pc-output="cloud">25%</output></span><input type="range" min="0" max="80" step="5" value="25" data-pc-control="cloud"></label>
        <label><span>Rendering</span><select data-pc-control="render"><option value="">Loading options…</option></select></label>
        <label><span>Layer opacity <output data-pc-output="opacity">85%</output></span><input type="range" min="10" max="100" step="5" value="85" data-pc-control="opacity"></label>
        <div class="fwi-pc-theme-guidance" data-pc-theme-guidance></div>
        <div class="fwi-pc-map-actions"><button type="button" class="neon-btn" data-pc-action="load">Load evidence for current view</button><button type="button" class="ghost-btn" data-pc-action="clear">Clear evidence</button></div>
      </div>
      <div class="fwi-pc-evidence-summary" data-pc-summary>
        <div><small>Selected indicator</small><strong>${escapeHtml(firstIndicator.label)}</strong><span>Load the layer to inspect Bajaur or the selected map view.</span></div>
        <div><small>What it measures</small><strong>${escapeHtml(firstIndicator.evidenceClass)}</strong><span>${escapeHtml(firstIndicator.explanation)}</span></div>
        <div><small>How to use it</small><strong>Interpret with the legend</strong><span>Click the map after loading to request a location-specific value.</span></div>
        <div><small>Additional evidence needed</small><strong>Validation required</strong><span>${escapeHtml(firstIndicator.requiredEvidence)}</span></div>
      </div>
      <div class="fwi-pc-map-stage"><div id="${id}" class="fwi-pc-map-canvas" role="application" aria-label="Interactive climate evidence map"></div><aside class="fwi-pc-map-legend" data-pc-legend aria-live="polite"></aside><div class="fwi-pc-map-status" data-pc-status aria-live="polite"><span class="ready"></span><strong>Map ready.</strong> Select a theme, indicator and focused area, then load evidence.</div></div>
      <div class="fwi-pc-map-meta" data-pc-meta><div><small>Evidence theme</small><strong>${escapeHtml(THEMES[themeKey].label)}</strong></div><div><small>Indicator</small><strong>${escapeHtml(firstIndicator.label)}</strong></div><div><small>Operational status</small><strong>Live STAC interface</strong></div><div><small>Source platform</small><strong>Microsoft Planetary Computer</strong></div><div><small>Verification</small><strong>Field and time-series review required</strong></div></div>
      <div class="fwi-pc-map-notice"><strong>Use responsibly:</strong> the moisture and vegetation layers are satellite-derived indicators. They do not by themselves prove meteorological drought, water scarcity for households, restoration success, hazard severity or causation. Compare like seasons and add rainfall, hydrological, land-use and field evidence.</div>
    </div>`;
  }

  function initMap(root){
    if(typeof window.L==='undefined'){root.textContent='The interactive map library could not be loaded.';return}
    const id=`fwi-pc-map-${++instanceCounter}`;
    const dates=defaultDates();
    const themeKey=THEMES[root.dataset.theme]?root.dataset.theme:'forests-land-cover';
    makeShell(root,id,dates,themeKey);
    const presetKey=PRESETS[root.dataset.preset]?root.dataset.preset:'bajaur';
    const controls={theme:root.querySelector('[data-pc-control="theme"]'),indicator:root.querySelector('[data-pc-control="indicator"]'),geography:root.querySelector('[data-pc-control="geography"]'),collection:root.querySelector('[data-pc-control="collection"]'),start:root.querySelector('[data-pc-control="start"]'),end:root.querySelector('[data-pc-control="end"]'),cloud:root.querySelector('[data-pc-control="cloud"]'),render:root.querySelector('[data-pc-control="render"]'),opacity:root.querySelector('[data-pc-control="opacity"]'),load:root.querySelector('[data-pc-action="load"]'),clear:root.querySelector('[data-pc-action="clear"]'),cloudOutput:root.querySelector('[data-pc-output="cloud"]'),opacityOutput:root.querySelector('[data-pc-output="opacity"]'),guidance:root.querySelector('[data-pc-theme-guidance]'),summary:root.querySelector('[data-pc-summary]'),legend:root.querySelector('[data-pc-legend]'),status:root.querySelector('[data-pc-status]'),meta:root.querySelector('[data-pc-meta]')};
    controls.geography.value=presetKey;
    const initial=PRESETS[presetKey];
    const map=L.map(id,{zoomControl:true,preferCanvas:true}).setView(initial.center,initial.zoom);
    L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a> contributors'}).addTo(map);
    L.control.scale({imperial:false}).addTo(map);
    let extentLayer=null,imageryLayer=null,renderOptions=[],activePointBase='',activePointParams=null,lastMeta=null;
    function selectedTheme(){return THEMES[controls.theme.value]||THEMES['forests-land-cover']}
    function selectedIndicator(){return indicatorByKey(controls.theme.value,controls.indicator.value)}
    function selectedLegend(){return LEGENDS[selectedIndicator().legend]||LEGENDS.context}
    function setStatus(kind,title,message){controls.status.className=`fwi-pc-map-status ${kind||''}`;controls.status.innerHTML=`<span class="${kind||'ready'}"></span><strong>${escapeHtml(title)}</strong>${message?` ${escapeHtml(message)}`:''}`}
    function updateThemeGuidance(){const theme=selectedTheme(),indicator=selectedIndicator();controls.guidance.innerHTML=`<div><small>${escapeHtml(indicator.evidenceClass)}</small><strong>${escapeHtml(indicator.label)}</strong><span>${escapeHtml(indicator.explanation)} ${escapeHtml(theme.note)}</span></div><a href="${THEME_BASE}#${encodeURIComponent(controls.theme.value)}">Open full theme guide</a>`}
    function updateSummary(sample=null){const indicator=selectedIndicator();const sampleTitle=sample?sample.title:'No location sampled';const sampleText=sample?sample.text:'Load the evidence layer, then click the map to request a location-specific value.';controls.summary.innerHTML=`<div><small>Selected indicator</small><strong>${escapeHtml(indicator.label)}</strong><span>${escapeHtml(indicator.explanation)}</span></div><div><small>Selected location</small><strong>${escapeHtml(sampleTitle)}</strong><span>${escapeHtml(sampleText)}</span></div><div><small>How to read it</small><strong>Use the map legend</strong><span>${escapeHtml(selectedLegend().howTo)}</span></div><div><small>Additional evidence needed</small><strong>Validation required</strong><span>${escapeHtml(indicator.requiredEvidence)}</span></div>`}
    function renderLegend(loaded=false){const legend=selectedLegend();controls.legend.innerHTML=`<button type="button" class="fwi-pc-legend-toggle" data-pc-legend-toggle aria-expanded="true">Theme guide & legend</button><div class="fwi-pc-legend-body"><small>${escapeHtml(selectedTheme().label)}</small><h3>${escapeHtml(legend.title)}</h3><p class="fwi-pc-legend-state">${loaded?'Evidence layer loaded':'Select “Load evidence” to display this layer.'}</p>${legend.rows.length?`<div class="fwi-pc-legend-scale">${legend.rows.map(row=>`<div><i style="--legend-color:${row.color}"></i><span><strong>${escapeHtml(row.label)}</strong><small>${escapeHtml(row.range)}</small></span></div>`).join('')}</div>`:''}<p>${escapeHtml(legend.howTo)}</p><p class="fwi-pc-legend-warning"><strong>Important:</strong> ${escapeHtml(legend.warning)}</p></div>`;const toggle=controls.legend.querySelector('[data-pc-legend-toggle]');toggle.addEventListener('click',()=>{const collapsed=controls.legend.classList.toggle('collapsed');toggle.setAttribute('aria-expanded',String(!collapsed))})}
    function drawExtent(preset){if(extentLayer)map.removeLayer(extentLayer);const [west,south,east,north]=preset.bbox;extentLayer=L.rectangle([[south,west],[north,east]],{color:'#f2b544',weight:1.5,opacity:.9,fillOpacity:.035,dashArray:'7 6',interactive:false}).addTo(map)}
    function applyPreset(key){const preset=PRESETS[key]||PRESETS.bajaur;const [west,south,east,north]=preset.bbox;map.fitBounds([[south,west],[north,east]],{padding:[18,18],animate:true});drawExtent(preset);setStatus('ready','View updated.',`${preset.label} is a search extent; zoom or pan before loading if needed.`)}
    function currentBbox(){const bounds=map.getBounds();return [bounds.getWest(),bounds.getSouth(),bounds.getEast(),bounds.getNorth()].map(value=>Number(value.toFixed(6)))}
    function validateSearch(bbox){const [west,south,east,north]=bbox;if(!controls.start.value||!controls.end.value)throw new Error('Select both start and end dates.');if(controls.start.value>controls.end.value)throw new Error('The start date must be before the end date.');if((east-west)>MAX_LONGITUDE_SPAN||(north-south)>MAX_LATITUDE_SPAN||map.getZoom()<5)throw new Error('The current view is too large for high-resolution evidence. Choose Khyber Pakhtunkhwa, Merged Districts or Bajaur, or zoom in further.')}
    function updateMeta(data){const cards=[['Evidence theme',selectedTheme().label],['Indicator',selectedIndicator().label],['Evidence class',selectedIndicator().evidenceClass],['Dataset',data.dataset],['Display mode',data.mode],['Scenes matched',String(data.sceneCount)],['Observation range',data.observationRange],['Rendering',data.rendering],['Upstream provider',data.providers],['Collection licence',data.license],['Verification','Satellite indication; climate and field review required']];controls.meta.innerHTML=cards.map(([label,value])=>`<div><small>${escapeHtml(label)}</small><strong>${escapeHtml(value||'Not stated')}</strong></div>`).join('')}
    function clearPointSampling(){activePointBase='';activePointParams=null}
    function removeImagery(){if(imageryLayer){map.removeLayer(imageryLayer);imageryLayer=null}clearPointSampling();renderLegend(false)}
    function addTileLayer(tileJson,label){if(!tileJson||!Array.isArray(tileJson.tiles)||!tileJson.tiles[0])throw new Error('The tile service returned no usable map tiles.');removeImagery();imageryLayer=L.tileLayer(tileJson.tiles[0],{minZoom:Number.isFinite(tileJson.minzoom)?tileJson.minzoom:0,maxZoom:Number.isFinite(tileJson.maxzoom)?tileJson.maxzoom:24,opacity:Number(controls.opacity.value)/100,attribution:`${escapeHtml(label)} via <a href="https://planetarycomputer.microsoft.com/" target="_blank" rel="noopener">Microsoft Planetary Computer</a>`}).addTo(map);imageryLayer.bringToFront();if(extentLayer)extentLayer.bringToFront();renderLegend(true)}
    function selectBestItem(features){return [...features].sort((a,b)=>{const cloudA=Number(a.properties?.['eo:cloud_cover']??999),cloudB=Number(b.properties?.['eo:cloud_cover']??999);if(cloudA!==cloudB)return cloudA-cloudB;return new Date(b.properties?.datetime||0)-new Date(a.properties?.datetime||0)})[0]}
    function featureDate(feature){return feature?.properties?.datetime||feature?.properties?.start_datetime||''}
    function observationRange(features){const values=features.map(featureDate).filter(Boolean).map(value=>new Date(value)).filter(date=>!Number.isNaN(date.getTime())).sort((a,b)=>a-b);if(!values.length)return `${controls.start.value} to ${controls.end.value}`;return `${isoDate(values[0])} to ${isoDate(values[values.length-1])}`}
    function collectionProviders(collection){const providers=(collection?.providers||[]).map(provider=>provider.name).filter(Boolean);return providers.length?providers.join(', '):'Original dataset provider listed in collection metadata'}
    function setIndicatorOptions(){const items=indicatorList(controls.theme.value);controls.indicator.innerHTML=items.map(item=>`<option value="${item.key}">${escapeHtml(item.label)}</option>`).join('');controls.indicator.value=items[0].key;applyIndicator(false)}
    async function applyIndicator(reload=true){const indicator=selectedIndicator();controls.collection.value=indicator.collection||'sentinel-2-l2a';controls.collection.disabled=Boolean(indicator.lockCollection);removeImagery();lastMeta=null;updateThemeGuidance();updateSummary();renderLegend(false);await loadRenderOptions();if(reload)setStatus('ready',`${indicator.label} selected.`,indicator.requiredEvidence)}
    async function loadRenderOptions(){const indicator=selectedIndicator(),collection=controls.collection.value;controls.render.disabled=true;controls.render.innerHTML='<option value="">Loading options…</option>';renderOptions=[];if(indicator.render){renderOptions=[indicator.render]}else{try{const info=await requestJson(`${DATA_API}/mosaic/info?collection=${encodeURIComponent(collection)}`);renderOptions=(info.renderOptions||[]).filter(option=>option&&option.type==='raster-tile')}catch(error){renderOptions=[]}if(indicator.renderMatcher){const matched=renderOptions.filter(option=>indicator.renderMatcher.test(`${option.name||''} ${option.description||''}`));if(matched.length)renderOptions=matched}}if(!renderOptions.length)renderOptions=[{name:'Collection default',description:'Default visualization supplied by the collection',options:''}];controls.render.innerHTML=renderOptions.map((option,index)=>`<option value="${index}">${escapeHtml(option.name||`Rendering ${index+1}`)}</option>`).join('');controls.render.disabled=renderOptions.length===1}
    async function buildMosaic(searchParameters,renderConfig){const registered=await requestJson(`${DATA_API}/mosaic/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(searchParameters)});const tileLink=(registered.links||[]).find(link=>link.rel==='tilejson')?.href;if(!tileLink)throw new Error('The mosaic service did not return a TileJSON link.');const params=new URLSearchParams(renderConfig?.options||'');params.set('collection',controls.collection.value);const tileJson=await requestJson(`${tileLink}?${params.toString()}`);return {tileJson,pointBase:tileLink.replace(/\/tilejson\.json(?:\?.*)?$/,''),pointParams:params}}
    async function buildSingleItem(features,renderConfig){const selected=selectBestItem(features);if(!selected?.id)throw new Error('No suitable scene was available for fallback rendering.');const params=new URLSearchParams(renderConfig?.options||'');params.set('collection',controls.collection.value);params.set('item',selected.id);const tileJson=await requestJson(`${DATA_API}/item/tilejson.json?${params.toString()}`);return {tileJson,selected,pointBase:`${DATA_API}/item`,pointParams:params}}
    function numericValue(payload){const candidates=[];if(Array.isArray(payload?.values))candidates.push(...payload.values.flat(Infinity));if(Array.isArray(payload?.assets))payload.assets.forEach(asset=>{if(Array.isArray(asset?.values))candidates.push(...asset.values.flat(Infinity))});for(const value of candidates){const number=Number(value);if(Number.isFinite(number))return number}return null}
    function interpretValue(value){const key=selectedIndicator().key;if(key==='ndmi'){if(value<-.2)return 'Very low moisture signal';if(value<.1)return 'Low moisture signal';if(value<.25)return 'Transitional or mixed moisture signal';if(value<.4)return 'Moderate moisture signal';return 'High moisture signal'}if(key==='ndvi'){if(value<.1)return 'Very sparse or non-vegetated signal';if(value<.25)return 'Sparse vegetation signal';if(value<.45)return 'Moderate vegetation signal';if(value<.65)return 'Healthy vegetation signal';return 'Very strong vegetation signal'}return 'Context image; no classified index value'}
    async function samplePoint(latlng,openPopup=true){const indicator=selectedIndicator();if(!activePointBase||!activePointParams||indicator.key==='context'){const text=indicator.key==='context'?'Natural-colour context has no single drought value. Select NDMI or NDVI for an index.':'Load an operational indicator before sampling a location.';if(openPopup)L.popup().setLatLng(latlng).setContent(`<strong>${escapeHtml(indicator.label)}</strong><br>${escapeHtml(text)}`).openOn(map);updateSummary({title:'No numeric value',text});return}try{const params=new URLSearchParams(activePointParams.toString());const pointUrl=`${activePointBase}/point/${latlng.lng.toFixed(6)},${latlng.lat.toFixed(6)}?${params.toString()}`;const payload=await requestJson(pointUrl);const value=numericValue(payload);if(value===null)throw new Error('No valid pixel value was returned for this location.');const category=interpretValue(value);updateSummary({title:`${category} (${value.toFixed(3)})`,text:`Location ${latlng.lat.toFixed(4)}, ${latlng.lng.toFixed(4)}. ${selectedIndicator().requiredEvidence}`});if(openPopup)L.popup({maxWidth:360}).setLatLng(latlng).setContent(`<strong>${escapeHtml(indicator.label)}</strong><br><b>Value:</b> ${value.toFixed(3)}<br><b>Interpretation:</b> ${escapeHtml(category)}<br><small>${escapeHtml(selectedLegend().warning)}</small>`).openOn(map)}catch(error){const text='The map layer loaded, but the public point-sampling service did not return a usable value. Read the colour legend and try another location.';updateSummary({title:'Point value unavailable',text});if(openPopup)L.popup().setLatLng(latlng).setContent(`<strong>${escapeHtml(indicator.label)}</strong><br>${escapeHtml(text)}`).openOn(map)}}
    async function loadImagery(){const bbox=currentBbox();try{validateSearch(bbox)}catch(error){setStatus('error','Search not started.',error.message);return}controls.load.disabled=true;controls.load.textContent='Loading thematic evidence…';setStatus('loading',`Loading ${selectedIndicator().label}…`,'Planetary Computer processing may take several seconds.');const collectionId=controls.collection.value,collectionConfig=COLLECTIONS[collectionId];const searchParameters={collections:[collectionId],bbox,datetime:`${controls.start.value}/${controls.end.value}`,limit:100};if(collectionConfig?.cloud)searchParameters.query={'eo:cloud_cover':{lt:Number(controls.cloud.value)}};try{const [results,collectionMeta]=await Promise.all([requestJson(`${STAC_API}/search`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(searchParameters)}),requestJson(`${STAC_API}/collections/${encodeURIComponent(collectionId)}`).catch(()=>null)]);const features=Array.isArray(results.features)?results.features:[];if(!features.length)throw new Error('No matching scenes were found. Expand the date range, increase cloud tolerance or move the map.');const selectedRender=renderOptions[Number(controls.render.value)]||renderOptions[0]||{name:'Collection default',options:''};let mode='Planetary Computer mosaic',result,selectedItem=null;try{result=await buildMosaic(searchParameters,selectedRender)}catch(mosaicError){const fallback=await buildSingleItem(features,selectedRender);result=fallback;selectedItem=fallback.selected;mode='Best single-scene fallback'}const datasetName=collectionMeta?.title||COLLECTIONS[collectionId]?.label||collectionId;addTileLayer(result.tileJson,datasetName);activePointBase=result.pointBase;activePointParams=result.pointParams;const sceneCount=Number(results.context?.matched??results.numberMatched??features.length);lastMeta={dataset:datasetName,mode,sceneCount,observationRange:observationRange(features),rendering:selectedRender.name,providers:collectionProviders(collectionMeta),license:collectionMeta?.license||'See collection metadata'};updateMeta(lastMeta);const selectedNote=selectedItem?` Scene: ${selectedItem.id}.`:'';setStatus('success','Evidence layer loaded.',`${selectedIndicator().label}; ${mode}; ${sceneCount} matching scene${sceneCount===1?'':'s'}.${selectedNote} Click the map for a location value.`);await samplePoint(map.getCenter(),false)}catch(error){console.error('FWI Planetary Computer thematic map error',error);setStatus('error','Unable to load thematic evidence.',error.name==='AbortError'?'The request timed out. Please try a smaller area.':error.message)}finally{controls.load.disabled=false;controls.load.textContent='Load evidence for current view'}}
    controls.theme.addEventListener('change',()=>{setIndicatorOptions();setStatus('ready',`${selectedTheme().label} selected.`,selectedTheme().note)});
    controls.indicator.addEventListener('change',()=>applyIndicator(true));
    controls.geography.addEventListener('change',event=>applyPreset(event.target.value));
    controls.collection.addEventListener('change',()=>{removeImagery();lastMeta=null;loadRenderOptions();setStatus('ready','Dataset changed.','Load evidence to refresh the map.')});
    controls.render.addEventListener('change',()=>{removeImagery();lastMeta=null;setStatus('ready','Rendering changed.','Load evidence to refresh the map.')});
    controls.cloud.addEventListener('input',()=>{controls.cloudOutput.textContent=`${controls.cloud.value}%`});
    controls.opacity.addEventListener('input',()=>{controls.opacityOutput.textContent=`${controls.opacity.value}%`;if(imageryLayer)imageryLayer.setOpacity(Number(controls.opacity.value)/100)});
    controls.load.addEventListener('click',loadImagery);
    controls.clear.addEventListener('click',()=>{removeImagery();lastMeta=null;updateSummary();setStatus('ready','Evidence cleared.','The base map remains active.')});
    map.on('click',event=>samplePoint(event.latlng,true));
    map.on('moveend',()=>{const selected=PRESETS[controls.geography.value];if(selected){const center=map.getCenter();if(Math.abs(center.lat-selected.center[0])>.7||Math.abs(center.lng-selected.center[1])>.7)controls.geography.value=''}});
    updateThemeGuidance();updateSummary();renderLegend(false);applyPreset(presetKey);applyIndicator(false);setTimeout(()=>map.invalidateSize(),250);
  }

  function simplifyThemeNavigation(){document.querySelectorAll('.fwi-theme-nav-menu').forEach(menu=>{const group=menu.closest('.fwi-evidence-nav-group');if(!group)return;const link=group.querySelector(':scope > a');if(link){link.classList.add('fwi-theme-index-link');group.replaceWith(link)}})}
  function init(){simplifyThemeNavigation();document.querySelectorAll('[data-fwi-planetary-map]').forEach(initMap)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
